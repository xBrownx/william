import styled from 'styled-components'
import { ButtonProps } from "./props";
import * as React from 'react';
import { colour, width, height, fontSize } from '@/utils'

export const StyledButton = styled.div<ButtonProps>`
    
`