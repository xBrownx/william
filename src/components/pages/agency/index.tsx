import * as React from "react";
import Page from "../../templates/page";
import { agencyConst as CONST } from './constants'
import { RouteProps } from "../props";

const Agency: React.FC<RouteProps> = (props) => {
    return (
        <Page>

        </Page>
    )
}

export default React.memo(Agency);